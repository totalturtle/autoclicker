# AutoClicker 개발 로그

## 주요 기능 목록

### 기본 동작
- 접근성 서비스(AccessibilityService) 기반 자동 클릭
- 오버레이 플로팅 패널 + 드래그 가능한 포인트 마커
- 볼륨 버튼 핫키로 시작/정지

### 포인트 설정
- X/Y 좌표, 이름, 딜레이, 랜덤 오차(ms), 좌표 오차(px) 설정
- 제스처 타입: 탭 / 롱프레스 / 스와이프 (꺾임 경유점 포함)
- 포인트별 반복 횟수 / 시간 기반 반복(DURATION 모드)
- 포인트별 루프 종료 트리거 (stopLoopOnExecute)
- **포인트 순서 변경**: 설정 다이얼로그에서 번호 입력 → 해당 위치로 이동

### 트리거 조건
- 픽셀 색상 트리거: 지정 좌표의 색상 일치 여부 확인
- 이미지 영역 트리거: 드래그 캡처한 영역 이미지 매칭
- 트리거 미일치 시 동작: SKIP(건너뜀) / WAIT_RETRY(재시도)
- 색상 피커: 오버레이에서 화면 색상 직접 샘플링

### 전체 반복 설정
- 무한 반복 / 횟수 지정 / 시간 기반(초·분·시간)
- 전체 딜레이 일괄 적용, 전체 오차 일괄 적용

### 프로필
- 현재 설정 저장 / 불러오기 / 삭제
- JSON 파일로 내보내기 / 가져오기
- 앱 연동: 특정 앱 실행 시 해당 프로필 자동 로드
- **드래그 앤 드롭 순서 변경**
- 오버레이에서 바로 저장/로드 가능

### 앱 연동 (App Linking)
- AccessibilityService `TYPE_WINDOW_STATE_CHANGED` / `TYPE_WINDOWS_CHANGED` 이벤트 감지
- UsageStatsManager 폴링 폴백 (2초 간격)
- 연동된 앱 실행 시 오버레이 자동 표시 + 해당 프로필 자동 로드
- 동일 앱에 여러 프로필 연동 시 첫 번째 프로필 우선 적용
- 삼성 기기 패키지명 주의: 포켓몬GO → `com.nianticlabs.pokemongo.ares`, 틱톡라이트 → `com.ss.android.ugc.tiktok.lite`

### 오버레이 패널
- 최소화(▷)/펼치기 토글 (드래그 핸들 탭)
- 편집 모드: 포인트 추가/삭제/드래그 이동
- 마커 숨기기/보이기 토글
- 포인트 초기화, 앱 종료 버튼
- **기본 시작 상태: 패널 최소화 + 마커 숨김**
- **기본 위치: 화면 우측 중앙 (가장자리에서 12dp 이격)**
- 오버레이에서 색상 피커, 영역 캡처 버튼 제공

### 통계
- 클릭 횟수 실시간 표시
- StatsActivity에서 상세 통계 확인

---

## 커밋 히스토리 요약

| 커밋 | 내용 |
|------|------|
| becbc94 | 포인트 순서 변경, 프로필 드래그 정렬, 오버레이 초기 상태(최소화+마커숨김) |
| ed6cf7a | 앱 연동 시 오버레이 자동 표시, 게임 앱 감지 개선 |
| efa9fc3 | 프로필 메뉴 진입 시 크래시 수정 (대용량 Intent 전달 제거) |
| cf6d492 | 포인트별 반복 루프 종료 트리거 추가 |
| 609afe3 | UI 개선: 포인트 설정 레이아웃 정리, 상태바 대응, 픽셀오차 전체 적용 |
| aa1f2bc | UX 개선: 접기/펼치기, 프로필 저장·로드, 좌표 랜덤 오차, UI 가독성 |
| 92f1b37 | 시간 기반 반복(DURATION) 모드 추가, 오버레이 다이얼로그 저장 버그 수정 |
| 010b724 | 이미지 영역 트리거 드래그 캡처 구현 및 크리티컬 버그 수정 |
| 3c898a9 | 색상 트리거 오동작 수정 및 UI 정리 |
| fdaab5c | 색상 트리거 버그 수정 및 UX 개선 |
| 801db44 | 포인트 반복, 정지 안정화, 스와이프 끝점 마커, 재생 버그 수정 |
| da450ec | 정지 버튼 신뢰성 개선, 메인 시작/정지 버튼 제거 |
| f04b7d6 | 색상 피커 기능 추가 |

---

## 알려진 주의사항

- `PACKAGE_USAGE_STATS` 권한은 설정 → 앱 → 특별한 앱 접근 → 사용 기록 접근에서 수동 허용 필요
- 접근성 서비스 활성화 필수 (자동 클릭 + 앱 연동 모두)
- 삼성 기기에서 패키지명이 일반 패키지명과 다를 수 있음
- 오버레이 패널 위치는 처음 실행 시에만 기본값 적용 (이후 드래그로 이동 가능)
